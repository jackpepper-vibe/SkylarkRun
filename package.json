{
  "name": "skylark-run",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Open-cockpit air racing over sunlit countryside.",
  "scripts": {
    "test": "node tools/check-modules.mjs && node tools/headless-test.mjs",
    "check": "node tools/check-modules.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
